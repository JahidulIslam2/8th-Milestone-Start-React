import React from 'react';

const Step = (props) => {
    return (
        <div>
            <h3>Step change another component</h3>
            <h2>{props.Step}</h2>
        </div>
    );
};

export default Step;